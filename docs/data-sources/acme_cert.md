---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "openwrt_acme_cert Data Source - openwrt"
subcategory: ""
description: |-
  ACME-managed certificate configuration (UCI: /etc/config/acme, section type: cert). Each section manages one certificate with one or more domain names.
---

# openwrt_acme_cert (Data Source)

ACME-managed certificate configuration (UCI: /etc/config/acme, section type: cert). Each section manages one certificate with one or more domain names.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) Name of the section. This name is only used when interacting with UCI directly.

### Read-Only

- `acme_server` (String) URL of the ACME directory endpoint to use as the CA. Defaults to Let's Encrypt when unset.
- `calias` (String) Challenge alias domain: all DNS challenge records are written under this domain instead of the actual certificate domain. Used with the DNS alias mode.
- `cert_profile` (String) Certificate profile to request from the CA (provider-specific, e.g. "tlsserver").
- `credentials` (List of String, Sensitive) DNS provider API credentials as shell-variable assignments (e.g. ["CF_Email=user@example.com", "CF_Key=abc123"]). Required when validation_method is "dns".
- `dalias` (String) Domain alias: the certificate's DNS records are managed under this domain. Similar to calias but applies to all challenge types.
- `days` (Number) Number of days before certificate expiry at which renewal is attempted. Default: 60.
- `dns` (String) DNS API provider to use for DNS-01 challenges (e.g. "dns_cf" for Cloudflare, "dns_duckdns" for DuckDNS). Required when validation_method is "dns".
- `dns_wait` (Number) Seconds to wait for DNS propagation before asking the CA to validate. Increase this if validation fails due to slow DNS propagation.
- `domains` (List of String) List of domain names to include in the certificate. The first entry becomes the Common Name; subsequent entries become Subject Alternative Names. Wildcard domains (e.g. "*.example.com") require DNS validation.
- `enabled` (Boolean) Enable or disable management of this certificate.
- `key_type` (String) Key algorithm and size. One of "rsa2048", "rsa3072", "rsa4096", "ec256", or "ec384".
- `listen_port` (Number) TCP port for the temporary ACME challenge HTTP server. Used by standalone and ALPN validation methods. Defaults to 80 (standalone/webroot) or 443 (ALPN).
- `staging` (Boolean) Use the Let's Encrypt staging environment. Certificates issued will not be trusted by browsers but allow testing without rate-limit concerns.
- `validation_method` (String) ACME challenge type: "standalone" (built-in HTTP server), "webroot" (existing web server), "dns" (DNS-01 via provider API), or "alpn" (TLS-ALPN-01 on port 443).
- `webroot` (String) Filesystem path served by the existing web server where ACME challenge files are written. Used when validation_method is "webroot". Default: "/var/run/acme/challenge/".


